import styled from "styled-components";

const CatalogPageRoot = styled.div`
  width: 1035px;
  background-color: #fff;
  height: 850px;
  overflow: hidden;
`;

const Frame = () => {
  return <CatalogPageRoot />;
};

export default Frame;
